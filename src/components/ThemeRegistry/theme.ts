import type { ThemeConfig } from "antd";

import { theme } from "antd";

const { darkAlgorithm, defaultAlgorithm } = theme;

const themee: ThemeConfig | undefined = {
	token: {
		fontSize: 16,
		colorPrimary: "#95182E",
	},
	algorithm: [],
};

export default themee;
